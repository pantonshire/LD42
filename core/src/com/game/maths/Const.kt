package com.game.maths

object Const {

    /* 2π, which should replace π in my opinion :P */
    const val TAU: Double = Math.PI * 2

    /* Multiply a value in radians by this value to convert to degrees. */
    const val TO_DEGREES: Double = 360.0 / TAU

}